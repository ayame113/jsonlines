{
  "tasks": {
    "test": "deno test --allow-read=.",
    "test:coverage": "deno test --allow-read=. --coverage=coverage",
    "test:lint": "deno lint",
    "test:fmt": "deno fmt --check",
    "fmt": "deno fmt",
    "check": "deno check ./mod.ts",
    "transpile": "deno run --allow-read=. --allow-write=. ./dev/transpile.ts && deno task fmt && deno task test"
  }
}
